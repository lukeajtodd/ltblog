- for item, index in articles
  .article
    a(href=`/${index}`)
      h2.article__title= item.title
    p.article__description= item.description
    img.article__image(src="#{item.screengrab}") 
    .separator
